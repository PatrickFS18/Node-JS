<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Perguntas sobre Funcionários</title>
   <style>
    body {
      font-family: Arial, sans-serif;
      text-align: center;
      margin: 0;
      padding: 20px;
      background-color: #f5f5f5;
    }

    h1 {
      margin-bottom: 20px;
    }

    h2 {
      margin-top: 30px;
    }

    ul {
      list-style-type: none;
      padding: 0;
    }

    li {
      margin-bottom: 10px;
    }

    form {
      margin-top: 20px;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    input[type="text"] {
      padding: 5px;
      width: 300px;
      border: 1px solid #ccc;
      border-radius: 5px;
    }

    button[type="submit"] {
      padding: 5px 20px;
      background-color: #007bff;
      color: white;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    button[type="submit"]:hover {
      background-color: #0056b3;
    }

    #adminCount {
      font-weight: bold;
    }
  </style>
</head>
<body>
  <h1>Perguntas sobre Funcionários</h1>

  <!-- Quem ganha o maior salário -->
  <h2>Quem ganha o maior salário?</h2>
  <p id="highestSalary">{{highestSalary}}</p>

  <!-- Quem ganha o menor salário -->
  <h2>Quem ganha o menor salário?</h2>
  <ul>
    {{#each employeesWithLowestSalary}}
      <li>{{this.nome}}</li>
    {{/each}}
  </ul>

  <!-- Pesquisar nome por pedaço de nome -->
  <h2>Pesquisar nome por pedaço de nome</h2>
  <form action="/searchBy" method="post">
    <input type="text" name="nome" placeholder="Digite um pedaço do nome">
    <button type="submit">Pesquisar</button>
  </form>

  
    

  <ul>
    {{#each searchResults}}
      <li>{{this.nome}}</li>
    {{/each}}
  </ul>

  <!-- Classificar pelo setor de trabalho -->
  <h2>Classificar pelo setor de trabalho</h2>
  <ul>
    <ul>
  {{#each sortedByDepartment}}
    <li>{{this.nome}} - {{this.departmentName}}</li>
  {{/each}}
</ul>

  <!-- Quantos trabalham no setor administrativo? -->
  <h2>Quantos trabalham no setor administrativo?</h2>
  <p id="adminCount">{{adminCount}} funcionários</p>
</body>
</html>
